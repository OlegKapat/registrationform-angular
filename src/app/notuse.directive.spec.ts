import { NotuseDirective } from './notuse.directive';

describe('NotuseDirective', () => {
  it('should create an instance', () => {
    const directive = new NotuseDirective();
    expect(directive).toBeTruthy();
  });
});
